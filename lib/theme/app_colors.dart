import 'package:flutter/material.dart';

class AppColors {
  // Core backgrounds
  static const bgDeep = Color(0xFF0B0D10);
  static const bgSurface = Color(0xFF111318);
  static const plumFaint = Color(0xFF1A0D22);
  static const plumMid = Color(0xFF2A1438);

  // Accent colors
  static const amberGlow = Color(0xFFFFBF5E);
  static const amberDim = Color.fromRGBO(255, 191, 94, 0.15);
  static const signalGreen = Color(0xFF00F665);
  static const greenDim = Color.fromRGBO(0, 246, 101, 0.08);
  static const blazeOrange = Color(0xFFF66500);
  static const orangeDim = Color.fromRGBO(246, 101, 0, 0.08);
  static const errorRed = Color(0xFFFF4466);

  // Glass/surface
  static const glassPanel = Color.fromRGBO(255, 255, 255, 0.03);
  static const glassPanelHover = Color.fromRGBO(255, 255, 255, 0.06);
  static const glassStroke = Color.fromRGBO(255, 255, 255, 0.08);
  static const glassStrokeLight = Color.fromRGBO(255, 255, 255, 0.12);

  // Text
  static const textPrimary = Color(0xFFF5E8D8);
  static const textSecondary = Color.fromRGBO(245, 232, 216, 0.5);
  static const textTertiary = Color.fromRGBO(245, 232, 216, 0.3);
}
